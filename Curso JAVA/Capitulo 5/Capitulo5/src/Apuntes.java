public class Apuntes {
    public static void main(String[] args) throws Exception {
        
        /*
            Cadenas de caracteres

            Una cadena de caracteres, es la conformacion de un conjunto de letras, numeros, simbolos, etc..Dentro de unas comillas dobles, en java no existe un tipo de dato para definir a una variable de tipo cadena de caracterer, lo que existe es la clase String, que basicamente es una clase en la cual nos permite manipular cadenas de caracteres.

            *********
            Importante

            Cada vez que definimos una variable de tipo String, en realidad lo que estamos haciendo es crear una instancia de la clase String, basicamente no se va a comportar como una variable, sino mas bien como un objeto.
            *********

            Metodos de la clase String

            Como las cadenas de caracteres, son instancias de la clase String, en terminos generales un objeto, la clase String maneja un conjunto de metodos para manipular las cadenas de caracteres.

            En el siguiente enlace, estara todo el listado de los metodos:

            https://docs.oracle.com/javase/7/docs/api/java/lang/String.html
        */

        /*
            Caracteres para presentar de una mejor manera la informacion

            Cuando querramos imprimir informacion por consolo usando la instruccion System.out.print("");, Nosotros tendremos dos caracteres especiales para mostrar de una mejor manera la informacion.

            * Caracter \t ⇒ Se usa para crear una tabulacion a la hora de imprimir.

                Sintaxis => System.out.print("Este es un mensaje \t con tabulacion");

                Salida => Este es un mensaje       con tabulacion

            * Caracter \n ⇒ Se usa para crear un salto de linea a la hora que hagamos la impresion.

                Sintaxis => System.out.print("Esto es un \n mensaje en dos lineas");

                Salida => Esto es un 
                          mensaje en dos lineas
        */
    }
}
